// get the nodes from the LAN PENDING

// use the ips from nodes to the access by SSH. requieres: (user, pass)

// initialize cluster. requires: (settings, user, pass, nombreCluster)
// SSH must be active

// 